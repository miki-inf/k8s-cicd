apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-reference
  namespace: kube-system
data:
  dashboards.yaml: |
    - name: 'default'
      org_id: 1
      folder: ''
      type: file
      options:
        path: /var/lib/grafana/dashboards
